version: 1
frontend:
  phases:
    preBuild:
      commands:
        - pnpm run install
    build:
      commands:
        - pnpm run build
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
